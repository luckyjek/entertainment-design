import React from 'react';
import styles from './footer.module.css';

const footer = () => {
    return(
        <footer className={styles.footer}>
            <h4 className={styles.h4}> © Entertainment</h4>
        </footer>
    )
}

export default footer;