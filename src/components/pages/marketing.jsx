import React from 'react';
import Modal from '../modal/modal';

const Marketing = () =>{
  const name = 'Marketing';
  return(
      <>
          <Modal name={name}/>
      </>
  )
}

export default Marketing;